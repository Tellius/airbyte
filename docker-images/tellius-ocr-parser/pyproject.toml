[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tellius-ocr-parser"
version = "0.1.0"
description = "Tellius custom OCR parser for Airbyte file-based sources"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["tellius_ocr_parser*"]
